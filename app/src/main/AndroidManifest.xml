<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PokerDice"
        android:name=".PokerDice">
        <activity
            android:name=".ui.title.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PokerDice">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.about.AboutActivity"
            android:exported="false" />
        <activity android:name=".ui.profile.ProfileActivity"
            android:exported="false" />
        <activity android:name=".ui.lobby.lobbies.LobbiesActivity"
            android:exported="false" />
        <activity android:name=".ui.lobby.lobbyCreation.LobbyCreationActivity"
            android:exported="false"/>
        <activity android:name=".ui.lobby.lobbyIndividual.LobbyActivity"
            android:exported="false"/>
    </application>

</manifest>